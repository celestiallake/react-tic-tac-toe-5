const CHANGE_FIELD_SIZE = 0;
const MAKE_MOVE = 1;
const TOGGLE_SWITCH = 2;
const READ_INPUT = 3;
const RESET_FIELD = 4;

const types = {
  CHANGE_FIELD_SIZE,
  MAKE_MOVE,
  TOGGLE_SWITCH,
  READ_INPUT,
  RESET_FIELD,
};

export default types;
